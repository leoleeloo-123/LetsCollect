# Color Unicorn archive

Status: inactive experiment retained for possible future reuse.

- `source/` keeps the original high-resolution colored GLB and import notes. Source GLBs remain local and Git-ignored.
- `runtime-experiment/` keeps the optimized experimental GLB and protection mask.
- `code/` keeps the former clean-unicorn Lab page and viewer.

The clean Lab referenced the separate `jelly-jade-unicorn` rollback asset, which remains in the legacy crystal pool. Nothing in this directory is requested by the active frontend.