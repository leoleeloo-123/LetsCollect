# Color Dog archive

- `protect-masks/` stores superseded masks retained for rollback and regeneration.
- `mask-builders/build-color-dog-mask-v027.py` is the superseded builder retained for history.
- The active v028 builder remains in `scripts/3d/`.
