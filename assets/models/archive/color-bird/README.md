# Color Bird archive

- `masks/protect-mask-mobile-v001.webp` through `v013.webp` are superseded validation outputs.
- `masks/head-zone-v001.webp` is a historical zone experiment.
- `mask-builders/build-color-bird-mask-v011.py` is a superseded builder wrapper.
- The active v014 builder remains in `scripts/3d/`; its v001-v005 dependency chain stays beside it so the active output is reproducible.
