# Color Cat mask archive

Superseded mask binaries and comparison renders belong here.

Historical builder snapshots v001-v007 live in `../mask-builders/`. They document the validation path but are not the active rebuild entrypoint. The standalone active builder is `scripts/3d/build-color-cat-mask-v008.py`; its output lives under `public/models/toys/color-cat/`.
