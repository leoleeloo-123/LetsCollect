# Color Unicorn archive

Status: inactive experiment retained for possible future research.

- `source/` keeps the original high-resolution colored GLB and import notes.
- `runtime-experiment/` keeps the optimized experimental GLB and protection mask.
- `code/` keeps the former clean-unicorn Lab page and viewer.

The former Lab also referenced Jelly Jade Unicorn, whose runtime exports are now
fully offline under `assets/models/archive/jelly-jade-unicorn/runtime/`. Nothing
in this directory is requested by the active frontend.